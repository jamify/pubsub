export enum Event {
  CONNECT = 'connect',
  DISCONNECT = 'disconnect',
  CHANNEL = 'channel',
  PROPOGATE = 'propagate',
  TRACK = 'track',
}

export enum Level {
  ERROR = 'error',
  INFO = 'info',
}
